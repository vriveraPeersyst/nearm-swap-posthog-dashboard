{
  "buildCommand": "cd packages/api && npm install && npm run build && mkdir -p ../../api && cp dist/handler.js ../../api/swap-metrics.js && cd ../frontend && npm install && npm run build",
  "outputDirectory": "packages/frontend/dist",
  "devCommand": "cd packages/frontend && npm run dev",
  "installCommand": "npm install"
}
